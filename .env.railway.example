# ============================================
# RAILWAY POSTGRESQL CONNECTION
# ============================================
# Railway Dashboard → Database → Connect → Variables sekmesinden kopyala

# Railway Private Network (Önerilen - daha hızlı)
RAILWAY_PGHOST=your-railway-host.railway.internal
RAILWAY_PGUSER=postgres
RAILWAY_PGPASSWORD=your-railway-password
RAILWAY_PGDATABASE=railway
RAILWAY_PGPORT=5432

# Alternatif: Public Network (External erişim için)
# RAILWAY_PGHOST=your-railway-host.railway.app
# RAILWAY_PGPORT=5432

# ============================================
# LOCAL POSTGRESQL CONNECTION (Kaynak - Docker)
# ============================================
DB_HOST=localhost
DB_USER=minibar_user
DB_PASSWORD=minibar123
DB_NAME=minibar_takip
DB_PORT=5433
DB_TYPE=postgresql

# ============================================
# FLASK CONFIGURATION
# ============================================
SECRET_KEY=your-super-secret-key-change-this-in-production
FLASK_ENV=production

# ============================================
# MIGRATION NOTES
# ============================================
# 1. Railway Dashboard'dan PostgreSQL bilgilerini kopyala
# 2. Bu dosyayı .env.railway olarak kaydet
# 3. Local PostgreSQL Docker'ın çalıştığından emin ol
# 4. python migrate_to_railway.py --dry-run (test)
# 5. python migrate_to_railway.py (gerçek migration)
